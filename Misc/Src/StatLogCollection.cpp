#include "StatLogCollection.h"
/*
StatLogCollection::~StatLogCollection()
{
    for(uint i(0); i<m_list.size(); ++i)
        delete m_list[i];
}

StatLog *StatLogCollection::appendStatLog(std::string name)
{
    StatLog *newStatLog = new StatLog(name);
    m_list.push_back(newStatLog);
    return(newStatLog);
}

void StatLogCollection::print(IOSerialStream * flux, bool printName)
{
    for(uint i(0); i<m_list.size(); ++i)
        m_list[i]->print(flux,printName);
}

void StatLogCollection::resetAllStatLog()
{
    for(uint i(0); i<m_list.size(); ++i)
        m_list[i]->reset();
}

void StatLogCollection::setLogMinMax(bool log)
{
    for(uint i(0); i<m_list.size(); ++i)
        m_list[i]->setLogMinMax(log);
}

void StatLogCollection::setLogAverage(bool log)
{
    for(uint i(0); i<m_list.size(); ++i)
        m_list[i]->setLogAverage(log);
}
void StatLogCollection::setLogStdDev(bool log)
{
    for(uint i(0); i<m_list.size(); ++i)
        m_list[i]->setLogStdDev(log);
}

void StatLogCollection::setLogCount(bool log)
{
    for(uint i(0); i<m_list.size(); ++i)
        m_list[i]->setLogCount(log);
}
*/
